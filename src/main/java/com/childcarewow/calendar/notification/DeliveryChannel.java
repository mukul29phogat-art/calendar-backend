package com.childcarewow.calendar.notification;

/** Per locked decision D4, PUSH is included alongside APP + EMAIL (Firebase FCM in Series 11). */
public enum DeliveryChannel {
  APP,
  EMAIL,
  PUSH
}
