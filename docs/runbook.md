# Calendar backend runbook

Operational procedures: deploy, rollback, incident response, common debugging recipes. Populated incrementally as the system grows.

## Sections (placeholder — filled by their owning Parts)

### Deploy
*Filled by Part 0.11 (CD pipelines).*

### Rollback
*Filled by Part 0.11 (CD pipelines) + Part 12.4 (rollback drills).*

### Incident response
*Filled by Part 9.x (observability + alerting).*

### Common debugging recipes
*Filled organically as issues are encountered. Each recipe should include: symptom, suspected cause, verification command(s), fix.*

### Local-dev gotchas
*See parent repo `Events_CCW/CLAUDE.md` §0 for live-state shell PATH gotchas (Docker, AWS CLI, gcloud installed mid-session don't propagate to existing Bash shells).*
